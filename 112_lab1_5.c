#include<stdio.h>
int main()
{
    int num,n,i,max=0,count;
    scanf("%d" ,n);
    for(i=0;i<n;i++)
    {
        scanf("%d" ,&num);
        if(max<num)
        {
            max=num;
            count=num;
        }
    }
    printf
    return 0;
}
